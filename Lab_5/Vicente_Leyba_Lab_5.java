//The imports below are for I/Oimport java.util.*;import java.io.*;// Declare a public classpublic class Vicente_Leyba_Lab_5 {	public static void main(String[] args) {		// Create scanner		Scanner sc = new Scanner(System.in);		// Declare variables		int menuOption, creditHours, area, prefix, suffix, studentId;		double gpa;		char studentData;		String menuOption2;		//will store the contact info		Contact[] contactinfo = new Contact[3];		//this will store the number of contacts already stored		int contactsSoFar = 0;		// Initialize the variables		menuOption = 0;		// Get menu to display		do {			System.out.println("1) Enter a new contact");			System.out.println("2) Display the contacts");			System.out.println("3) Quit");			System.out.println("Selection:");			menuOption = sc.nextInt();			// Implement switch statement to			switch (menuOption) {			// If user enters 1 to create contact this will be displayed to user			case 1:				//if the maximum of 3 contacts entered is reached, print an error message				//end break from the switch				if( contactsSoFar >= 3 ){					System.out.println("ERROR! You allready have 3 records into the system, and that is the maximum allowed!");					break;				}								System.out.println("Enter the Student ID of this person: 900");				studentId = Integer.parseInt("900" + sc.next());								//create the contact that will store all the details				Contact contact = new Contact(studentId);				System.out.println("G)Enter The GPA For This Student");				System.out.println("H)Enter The Number Of Completed Credit Hours");								menuOption2 = sc.next().toUpperCase();				studentData = menuOption2.charAt(0);				// Prompts the user for the next choice				if (studentData == 'G') {					System.out.println("GPA: ");					gpa = sc.nextDouble();					//create the Data object					Data data = new Data();					data.setStudentGPA(gpa);					//add it to the contact					contact.setData(data);				} else if (studentData == 'H') {					System.out.println("Hours: ");					creditHours = sc.nextInt();					//create the Data object					Data data = new Data();					data.setStudentCreditHours(creditHours);					//add it to the contact					contact.setData(data);				} else {					System.out.println("Invalid Entry");				}				System.out.println("Enter The Phone Number of This Student");				System.out.println("(###)-###-####: ");				area = sc.nextInt();				prefix = sc.nextInt();				suffix = sc.nextInt();				//create the phone object				Phone_Number contactNumber = new Phone_Number(area, prefix,	suffix);				//add it to the contact				contact.setPhone(contactNumber);				//store the contact in the array at the next free location				contactinfo[ contactsSoFar ] = contact;				//increase the counter to point to the next free location (if any)				contactsSoFar++;				break;			case 2:				System.out.println("--------CONTACTS STORED---------");				//print each stored contact				for( int i = 0 ; i < contactsSoFar ; i++ )					System.out.println( (i+1) + ". " + contactinfo[ i ].toString() + "\n");				break;			}
		}		} while (menuOption != 3);}